{
    "validations": {
        "size_BKMG": {
            "description": "single escape is not possible",
            "args": [ "frame-size" ],
            "vals": "^(([1-9][0-9]*\.?[0-9]*)|(0?\.[0-9]+)).*[BKMG]?"
        }
    }
}
